import { TimeDifferPipe } from './time-differ.pipe';

describe('TimeDifferPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDifferPipe();
    expect(pipe).toBeTruthy();
  });
});
